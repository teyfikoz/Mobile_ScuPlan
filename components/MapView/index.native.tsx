// Native platform (iOS/Android) - use react-native-maps
export {
  default,
  Marker,
  Polyline,
  PROVIDER_GOOGLE,
} from 'react-native-maps';

export type { Region, MapMarkerProps, MapPolylineProps } from 'react-native-maps';
